{
  "name": "racehunter-acceptance-tests",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "playwright test",
    "test:real": "playwright test --config playwright.real.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
